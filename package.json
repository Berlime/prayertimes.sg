{
  "name": "prayertimes-sg",
  "version": "1.0.0",
  "description": "Prayer Times Singapore",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./public/styles.css --minify",
    "dev": "npx @tailwindcss/cli -i ./src/input.css -o ./public/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  }
}

